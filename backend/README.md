# Tikr Backend

FastAPI service placeholder for the deal pipeline + memo workspace.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
